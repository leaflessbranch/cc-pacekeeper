#!/usr/bin/env bash
# cc-pacekeeper hook entrypoint: precompact (PreCompact)
set -euo pipefail
DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" &> /dev/null && pwd)"
exec bun run --silent "$DIR/../src/precompact.ts"
