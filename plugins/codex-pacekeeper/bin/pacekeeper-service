#!/usr/bin/env bash
set -euo pipefail
if ! command -v bun >/dev/null 2>&1; then
  echo 'codex-pacekeeper: Bun is required for the optional service' >&2
  exit 1
fi
DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
exec bun run --silent "$DIR/../src/service.ts" "$@"
